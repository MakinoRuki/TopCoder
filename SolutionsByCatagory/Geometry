SRM 604 Div1 Hard(未coding)
给出50条线段，AB-CD，它们组成了一个图形。现要在10^9*10^9的方块纸上画这个图形多次，彼此之间是平移关系。问是否可以画无穷多次且使它们互不相交。
Solution：
如果平移之后相交，就意味着这个操作(dx,dy)使得图形上的一个点P->P+(dx,dy)之后与原图上的Q重合。
因此，不合法的(dx,dy)必是原图上两个不在同一条线段上的点P和Q的差。因此枚举线段S(AB)和S(CD)，四个端点的差组成平行四边形{A-C,A-D,B-D,B-C}。
设0<=s<=1,0<=t<=1,设P为S(AB)上的点则P=s*A+(1-s)*B,设Q为S(CD)上的点则Q=t*C+(1-t)*D，则任意一对(s,t)映射到平行四边形中的一个点。
这样所有n^2个平行四边形的union就组成一个区域，如果(0,0)在这个区域内，则存在一个小r，使得平移后两个图形的距离不得小于r，否则有overlap。
那么就不能画无穷多次。否则，如果(0,0)不在区域内，则可以画一条过(0,0)的线段，其上有无数个点，可以作为(dx,dy)，则可以画无穷多次。
corner case是，(0,0)在边界上。这样就需要判这些边界是否360度环绕(0,0)把它包裹起来使得它在区域内。

SRM 652 Div2 Hard
坐标平面内n<=50个点，一个人要按照某种顺序把每个点遍历一遍，要求轨迹不交叉，且每次只能左转。返回路径。
Solution：
从最下最左的点作为cur开始，每次选一个点next，使得所有未访问的点都在cur->next的向量左侧。依次贪心。

SRM 698 Div1 Med
n(n<=100)个点，从中选出集合的pair(s1, s2)满足，s1和s2不相交且两集合的凸多边形有公共面积。问方案数。
Solution：
补集思想。总方案数减去两集合凸多边形没有公共面积的方案数。也就是，如果没有公共面积，那么必然存在一条直线过两个多边形上各一个点，
能把两个多边形上的点分在直线的两边。那么就枚举这条直线，统计即可。

SRM 754 Div1 Easy
平面上n(n<=3000)个整点，问有多少个新的整点可以被添加进来，使得加进来之后C(n+1,4)选出的点组成的正方形个数比C(n,4)多。
Solution：
枚举一条边AB=(x0,y0)，求出与AB垂直的长度相等的向量(x0,-y0),(-x0,y0)，找出跟AB组成正方形的其余两点CD，不能同时出现在n个点中，
则把没出现的点放进一个set里，统计set大小。
